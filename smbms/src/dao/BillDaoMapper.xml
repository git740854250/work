<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
	namespace=""  命名空间，防止所有的包名不能重复
	值的规则：接口的完全包名.接口名
 -->
<mapper namespace="dao.BillDao">
	<select id="queryAllBill" resultType="pojo.Bill">
		select a.*,b.proName from smbms_bill as a,smbms_provider as b where a.providerId = b.id
		limit #{currentPageNo},#{pageSize}
	</select>
	<select id="queryBillNumber" resultType="int">
		select count(*) from smbms_bill
	</select>
	<select id="queryIdByBill" resultType="pojo.Bill">
		select a.*,b.proName from smbms_bill as a,smbms_provider as b where a.providerId = b.id and a.id = #{id}
	</select>
</mapper>